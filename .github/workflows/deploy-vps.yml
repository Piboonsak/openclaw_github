name: Deploy to Hostinger VPS

# Runs AFTER docker-build-push.yml pushes the image to Docker Hub.
# Triggers:
#   - Completion of the "Build and Push Docker Image" workflow on main
#   - Manual trigger (workflow_dispatch)
#
# Required GitHub Secrets (Settings → Secrets → Actions):
#   VPS_SSH_KEY             Primary SSH private key for VPS auth
#   DEPLOY_SSH_PRIVATE_KEY  Fallback SSH private key for VPS auth
#                           (contents of C:\Users\..\.ssh\id_ed25519_hostinger)
#   VPS_HOST             srv1414058.hstgr.cloud   (or 76.13.210.250)
#   VPS_USER             root
#   DOCKER_USERNAME      piboonsak  (already set for docker-build-push)
#   DOCKER_TOKEN         Docker Hub PAT (already set for docker-build-push)

on:
  workflow_run:
    workflows: ["Build and Push Docker Image"]
    types: [completed]
    branches: [main]

  workflow_dispatch:
    inputs:
      image_tag:
        description: "Docker image tag to deploy (default: latest)"
        required: false
        default: "latest"

concurrency:
  group: deploy-vps-${{ github.ref }}
  cancel-in-progress: false   # never cancel an in-flight deploy

jobs:
  deploy:
    name: Deploy piboonsak/openclaw to VPS
    runs-on: ubuntu-latest

    # Only deploy when the build workflow succeeded (skip on failure/cancel)
    if: >
      github.event_name == 'workflow_dispatch' ||
      github.event.workflow_run.conclusion == 'success'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Checkout Openclaw docs repository
        uses: actions/checkout@v4
        with:
          repository: Piboonsak/Openclaw
          path: openclaw-docs
          token: ${{ secrets.GITHUB_TOKEN }}

      # ── Create deployment log directory ────────────────────────────────────
      - name: Create deployment log directory
        run: |
          mkdir -p "operations/deployments/deploy-${{ github.run_number }}/logs"
          cat > "operations/deployments/deploy-${{ github.run_number }}/logs/metadata.json" << 'METADATA'
          {
            "run_id": "${{ github.run_id }}",
            "run_number": ${{ github.run_number }},
            "branch": "${{ github.ref }}",
            "commit_sha": "${{ github.sha }}",
            "timestamp": "$(date -u +'%Y-%m-%dT%H:%M:%SZ')",
            "workflow_file": "deploy-vps.yml",
            "repository": "${{ github.repository }}"
          }
          METADATA

      # ── Resolve image tag ──────────────────────────────────────────────────
      - name: Set image tag
        id: tag
        run: |
          TAG="${{ github.event.inputs.image_tag || 'latest' }}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "Deploying image: piboonsak/openclaw:${TAG}"

      # ── Write SSH key to disk (ephemeral runner) ───────────────────────────
      - name: Configure SSH key
        env:
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
          DEPLOY_SSH_PRIVATE_KEY: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh

          # Prefer VPS_SSH_KEY (operator-managed), fallback to DEPLOY_SSH_PRIVATE_KEY.
          SSH_KEY_CONTENT="${VPS_SSH_KEY:-${DEPLOY_SSH_PRIVATE_KEY:-}}"
          if [ -z "$SSH_KEY_CONTENT" ]; then
            echo "ERROR: Missing SSH private key secret. Set VPS_SSH_KEY (preferred) or DEPLOY_SSH_PRIVATE_KEY."
            exit 1
          fi

          printf '%s\n' "$SSH_KEY_CONTENT" > ~/.ssh/id_ed25519_hostinger
          chmod 600 ~/.ssh/id_ed25519_hostinger
          # Pre-approve host key to avoid interactive prompt
          # Retry ssh-keyscan in case VPS is transiently unreachable (Issue #110)
          # Allow failure — subsequent SSH commands use -o StrictHostKeyChecking=no
          for i in 1 2 3; do
            if ssh-keyscan -H "${{ secrets.VPS_HOST }}" >> ~/.ssh/known_hosts 2>/dev/null; then
              echo "✓ ssh-keyscan succeeded (attempt $i)"
              break
            fi
            echo "⚠ ssh-keyscan attempt $i/3 failed — retrying in 15s"
            sleep 15
          done || true
          # Verify key authentication works before proceeding
          AUTH_OK=0
          for i in 1 2 3; do
            if ssh -i ~/.ssh/id_ed25519_hostinger \
                 -o StrictHostKeyChecking=no \
                 -o ConnectTimeout=30 \
                 -o ServerAliveInterval=15 \
                 -o ServerAliveCountMax=2 \
                 "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" "echo 'SSH auth OK'"; then
              AUTH_OK=1
              break
            fi
            echo "⚠ SSH auth attempt $i/3 failed — retrying in 15s"
            sleep 15
          done
          if [ "$AUTH_OK" -ne 1 ]; then
            echo "ERROR: Cannot authenticate to VPS — check VPS_SSH_KEY/DEPLOY_SSH_PRIVATE_KEY and VPS network"
            exit 1
          fi

      # ── Copy the production nginx config to VPS and apply if valid ───────────
      - name: Sync and apply Nginx config to VPS
        run: |
          scp -i ~/.ssh/id_ed25519_hostinger \
              -o StrictHostKeyChecking=no \
              docker/nginx/openclaw.conf \
              "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/tmp/openclaw-nginx.conf"
          # Test and apply the new config (rollback to existing if nginx -t fails)
          ssh -i ~/.ssh/id_ed25519_hostinger -o StrictHostKeyChecking=no \
            "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" bash -s << 'NGINX_EOF'
          set -euo pipefail
          # Backup current config
          cp /etc/nginx/sites-available/openclaw /etc/nginx/sites-available/openclaw.bak.$(date +%Y%m%d%H%M%S) 2>/dev/null || true
          # Apply new config
          cp /tmp/openclaw-nginx.conf /etc/nginx/sites-available/openclaw
          ln -sf /etc/nginx/sites-available/openclaw /etc/nginx/sites-enabled/openclaw
          # Test and reload
          if nginx -t 2>/dev/null; then
            nginx -s reload
            echo "✓ Nginx config applied and reloaded successfully"
          else
            echo "ERROR: Nginx config test failed — rolling back"
            # Restore backup
            LATEST_BAK=$(ls -t /etc/nginx/sites-available/openclaw.bak.* 2>/dev/null | head -1)
            if [ -n "$LATEST_BAK" ]; then
              cp "$LATEST_BAK" /etc/nginx/sites-available/openclaw
              nginx -s reload
              echo "Rolled back to previous config"
            fi
            exit 1
          fi
          NGINX_EOF

      # NOTE: The VPS docker-compose lives at /docker/openclaw-sgnl/docker-compose.yml
      # deploy.sh now syncs docker/docker-compose.prod.yml from repo to VPS on every deploy
      # (Issue #67), then updates the image tag in-place. This ensures new volume mounts,
      # env_file directives, and environment variables reach the VPS.

      # ── Sync runtime secrets into VPS .env (CI/CD-managed) ───────────────
      - name: Sync runtime secrets to VPS env
        env:
          ANTHROPIC_API_KEY: ${{ secrets['ANTHROPIC_API_KEY'] }}
          BRAVE_API_ANSWER_KEY: ${{ secrets['BRAVE_API_ANSWER_KEY'] }}
          BRAVE_API_KEY: ${{ secrets['BRAVE_API_KEY'] }}
          BRAVE_API_SEARCH_KEY: ${{ secrets['BRAVE_API_SEARCH_KEY'] }}
          # Use workflow-scoped token for runtime gh auth sync.
          # If long-lived gh access is required, replace with an operator PAT secret.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets['LINE_CHANNEL_ACCESS_TOKEN'] }}
          LINE_CHANNEL_SECRET: ${{ secrets['LINE_CHANNEL_SECRET'] }}
          OPENAI_API_KEY: ${{ secrets['OPENAI_API_KEY'] }}
          OPENCLAW_GATEWAY_TOKEN: ${{ secrets['OPENCLAW_GATEWAY_TOKEN'] }}
          OPENROUTER_API_KEY: ${{ secrets['OPENROUTER_API_KEY'] }}
        run: |
          set -euo pipefail

          TMPDIR=$(mktemp -d)
          SECRETS_FILE="$TMPDIR/openclaw-runtime-secrets.env"
          touch "$SECRETS_FILE"
          chmod 600 "$SECRETS_FILE"

          MISSING_KEYS=()
          add_secret() {
            local key="$1"
            local value="$2"
            if [ -z "$value" ]; then
              MISSING_KEYS+=("$key")
              return
            fi
            printf '%s=%s\n' "$key" "$value" >> "$SECRETS_FILE"
          }

          add_secret "ANTHROPIC_API_KEY" "${ANTHROPIC_API_KEY:-}"
          add_secret "BRAVE_API_ANSWER_KEY" "${BRAVE_API_ANSWER_KEY:-}"
          add_secret "BRAVE_API_KEY" "${BRAVE_API_KEY:-}"
          add_secret "BRAVE_API_SEARCH_KEY" "${BRAVE_API_SEARCH_KEY:-}"
          add_secret "GITHUB_TOKEN" "${GITHUB_TOKEN:-}"
          add_secret "GH_TOKEN" "${GH_TOKEN:-}"
          add_secret "LINE_CHANNEL_ACCESS_TOKEN" "${LINE_CHANNEL_ACCESS_TOKEN:-}"
          add_secret "LINE_CHANNEL_SECRET" "${LINE_CHANNEL_SECRET:-}"
          add_secret "OPENAI_API_KEY" "${OPENAI_API_KEY:-}"
          add_secret "OPENCLAW_GATEWAY_TOKEN" "${OPENCLAW_GATEWAY_TOKEN:-}"
          add_secret "OPENROUTER_API_KEY" "${OPENROUTER_API_KEY:-}"

          if [ "${#MISSING_KEYS[@]}" -gt 0 ]; then
            echo "ERROR: Missing required GitHub secrets: ${MISSING_KEYS[*]}"
            rm -rf "$TMPDIR"
            exit 1
          fi

          scp -i ~/.ssh/id_ed25519_hostinger \
              -o StrictHostKeyChecking=no \
              -o ConnectTimeout=30 \
              -o ServerAliveInterval=15 \
              -o ServerAliveCountMax=4 \
              "$SECRETS_FILE" \
              "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/tmp/openclaw-runtime-secrets.env"

          timeout 180 ssh -i ~/.ssh/id_ed25519_hostinger -o StrictHostKeyChecking=no \
            -o ConnectTimeout=30 \
            -o ServerAliveInterval=15 \
            -o ServerAliveCountMax=4 \
            "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" bash -s << 'RUNTIME_SECRETS_EOF'
          set -euo pipefail
          APP_DIR="/docker/openclaw-sgnl"
          ENV_FILE="$APP_DIR/.env"
          SECRETS_FILE="/tmp/openclaw-runtime-secrets.env"

          if [ ! -s "$SECRETS_FILE" ]; then
            echo "ERROR: /tmp/openclaw-runtime-secrets.env missing on VPS"
            exit 1
          fi

          touch "$ENV_FILE"
          chmod 600 "$ENV_FILE"

          while IFS= read -r line || [ -n "$line" ]; do
            if [ -z "$line" ]; then
              continue
            fi

            # Preserve secret values exactly (including trailing '='),
            # so parse key/value by splitting only on the first '='.
            key="${line%%=*}"
            value="${line#*=}"

            if [ -z "$key" ]; then
              continue
            fi

            awk -v k="$key" -v v="$value" '
              BEGIN { updated = 0 }
              $0 ~ "^" k "=" {
                print k "=" v
                updated = 1
                next
              }
              { print }
              END {
                if (!updated) print k "=" v
              }
            ' "$ENV_FILE" > "$ENV_FILE.tmp"

            mv "$ENV_FILE.tmp" "$ENV_FILE"
          done < "$SECRETS_FILE"

          chmod 600 "$ENV_FILE"
          rm -f "$SECRETS_FILE"
          echo "✓ Runtime secrets synced to $ENV_FILE"
          RUNTIME_SECRETS_EOF

          rm -rf "$TMPDIR"

      # ── Run deploy.sh on VPS ───────────────────────────────────────────────
      - name: Deploy container
        continue-on-error: true
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          # SSH_KEY intentionally omitted: deploy.sh defaults to $HOME/.ssh/id_ed25519_hostinger
          # (tilde is not variable-expanded in GitHub Actions env values)
          IMAGE_TAG: ${{ steps.tag.outputs.tag }}
          APP_DIR: /docker/openclaw-sgnl
        run: |
          bash docker/deploy.sh "$IMAGE_TAG"

      # ── Sync model config from repo to VPS (selective merge) ─────────────
      # The VPS config at /data/.openclaw/openclaw.json contains runtime state
      # (meta, wizard, plugins, commands) that must be preserved.
      # We selectively merge ONLY the model-related fields from the repo template.
      - name: Sync model config to VPS
        run: |
          # Install json5 with pinned version to avoid npm resolution failures (Issue #109)
          TMPDIR=$(mktemp -d)
          echo "Installing json5@2.2.3 in isolated temp dir..."
          if ! (cd "$TMPDIR" && npm init -y --silent && npm install --silent --no-save json5@2.2.3); then
            echo "ERROR: npm install json5 failed — cannot parse JSON5 config"
            rm -rf "$TMPDIR"
            exit 1
          fi

          # Extract model config + token budget config from repo's JSON5 template
          NODE_PATH="$TMPDIR/node_modules" node -e "
            const json5 = require('json5');
            const fs = require('fs');
            const src = fs.readFileSync('config/openclaw.prod.json5', 'utf8');
            const cfg = json5.parse(src);
            const d = cfg.agents.defaults;
            const exec = cfg.tools?.exec ?? {};
            const patch = {
              model: d.model,
              models: d.models,
              contextTokens: d.contextTokens,
              bootstrapMaxChars: d.bootstrapMaxChars,
              bootstrapTotalMaxChars: d.bootstrapTotalMaxChars,
              promptTokenBudget: d.promptTokenBudget,
              execAsk: exec.ask,
              execSecurity: exec.security,
              execSafeBins: exec.safeBins,
              execSafeBinProfiles: exec.safeBinProfiles,
            };
            fs.writeFileSync('/tmp/model-patch.json', JSON.stringify(patch, null, 2));
            console.log('Patch extracted:', JSON.stringify({ model: patch.model, contextTokens: patch.contextTokens, bootstrapTotalMaxChars: patch.bootstrapTotalMaxChars, execAsk: patch.execAsk, execSecurity: patch.execSecurity, execSafeBinsCount: Array.isArray(patch.execSafeBins) ? patch.execSafeBins.length : 0, execSafeBinProfilesCount: patch.execSafeBinProfiles ? Object.keys(patch.execSafeBinProfiles).length : 0 }, null, 2));
          "
          rm -rf "$TMPDIR"

          # Upload patch to VPS
          scp -i ~/.ssh/id_ed25519_hostinger \
              -o StrictHostKeyChecking=no \
              /tmp/model-patch.json \
              "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/tmp/model-patch.json"

          # Merge patch into running config using jq, then restart container
          ssh -i ~/.ssh/id_ed25519_hostinger -o StrictHostKeyChecking=no \
            "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" bash -s << 'CONFIG_EOF'
          set -euo pipefail
          CONTAINER="openclaw-sgnl-openclaw-1"
          VOL_PATH=$(docker volume inspect openclaw-sgnl_openclaw-state --format '{{ .Mountpoint }}' 2>/dev/null || true)

          if [ -z "$VOL_PATH" ]; then
            echo "WARNING: Could not find openclaw-state volume — skipping config sync"
            exit 0
          fi

          CONFIG_FILE="$VOL_PATH/openclaw.json"
          PATCH_FILE="/tmp/model-patch.json"

          if [ ! -f "$CONFIG_FILE" ]; then
            echo "WARNING: Config file not found at $CONFIG_FILE — skipping"
            exit 0
          fi

          # Backup current config
          cp "$CONFIG_FILE" "$CONFIG_FILE.bak.$(date +%Y%m%d%H%M%S)"
          echo "✓ Backed up existing config"

          # Selective merge: update model, models, and token budget settings
          jq --slurpfile patch "$PATCH_FILE" '
            .agents.defaults.model = $patch[0].model |
            .agents.defaults.models = $patch[0].models |
            .agents.defaults.contextTokens = $patch[0].contextTokens |
            .agents.defaults.bootstrapMaxChars = $patch[0].bootstrapMaxChars |
            .agents.defaults.bootstrapTotalMaxChars = $patch[0].bootstrapTotalMaxChars |
            .agents.defaults.promptTokenBudget = $patch[0].promptTokenBudget |
            .tools.exec.ask = ($patch[0].execAsk // .tools.exec.ask) |
            .tools.exec.security = ($patch[0].execSecurity // .tools.exec.security) |
            .tools.exec.safeBins = ($patch[0].execSafeBins // .tools.exec.safeBins) |
            .tools.exec.safeBinProfiles = ($patch[0].execSafeBinProfiles // .tools.exec.safeBinProfiles)
          ' "$CONFIG_FILE" > "$CONFIG_FILE.tmp"

          mv "$CONFIG_FILE.tmp" "$CONFIG_FILE"
          chown 1000:1000 "$CONFIG_FILE"
          echo "✓ Model config merged into $CONFIG_FILE"
          echo "  Primary model: $(jq -r '.agents.defaults.model.primary' "$CONFIG_FILE")"
          echo "  contextTokens: $(jq -r '.agents.defaults.contextTokens' "$CONFIG_FILE")"
          echo "  bootstrapTotalMaxChars: $(jq -r '.agents.defaults.bootstrapTotalMaxChars' "$CONFIG_FILE")"

          # Restart container to pick up new config
          cd /docker/openclaw-sgnl
          docker compose restart
          echo "✓ Container restarted to apply new config"

          # Wait for container to be healthy
          echo "Waiting for container health..."
          for i in $(seq 1 12); do
            STATUS=$(docker inspect --format='{{.State.Health.Status}}' "$CONTAINER" 2>/dev/null || echo "unknown")
            if [ "$STATUS" = "healthy" ]; then
              echo "✓ Container is healthy"
              break
            fi
            echo "  attempt $i/12: status=$STATUS — waiting 10s"
            sleep 10
          done

          # Verify config persistence (Issues #97, #100)
          echo ""
          echo "=== Config Verification ==="
          echo "Primary model: $(jq -r '.agents.defaults.model.primary' "$CONFIG_FILE")"
          echo "contextTokens: $(jq -r '.agents.defaults.contextTokens' "$CONFIG_FILE")"
          echo "bootstrapMaxChars: $(jq -r '.agents.defaults.bootstrapMaxChars' "$CONFIG_FILE")"
          echo "bootstrapTotalMaxChars: $(jq -r '.agents.defaults.bootstrapTotalMaxChars' "$CONFIG_FILE")"
          echo "tools.exec.ask: $(jq -r '.tools.exec.ask' "$CONFIG_FILE")"
          echo "tools.exec.security: $(jq -r '.tools.exec.security' "$CONFIG_FILE")"
          echo "safeBins count: $(jq -r '.tools.exec.safeBins | length' "$CONFIG_FILE")"
          echo "safeBins includes ps: $(jq -r '.tools.exec.safeBins | contains(["ps"])' "$CONFIG_FILE")"
          echo "safeBins includes git: $(jq -r '.tools.exec.safeBins | contains(["git"])' "$CONFIG_FILE")"
          echo "safeBins includes gh: $(jq -r '.tools.exec.safeBins | contains(["gh"])' "$CONFIG_FILE")"
          echo "Config file owner: $(stat -c '%U:%G' "$CONFIG_FILE")"
          echo "Config file permissions: $(stat -c '%a' "$CONFIG_FILE")"

          rm -f "$PATCH_FILE"
          CONFIG_EOF

      # ── Sync NongKung markdown workspace files from Openclaw repo ───────
      - name: Upload NongKung markdown files to VPS
        run: |
          set -euo pipefail
          SRC_DIR="openclaw-docs/bots/nongkung"
          OUT_TGZ="/tmp/nongkung-md.tgz"

          if [ ! -d "$SRC_DIR" ]; then
            echo "ERROR: Source directory not found: $SRC_DIR"
            exit 1
          fi

          # cd into source dir so *.md glob expands against the correct directory
          (cd "$SRC_DIR" && tar -czf "$OUT_TGZ" -- *.md)

          scp -i ~/.ssh/id_ed25519_hostinger \
              -o StrictHostKeyChecking=no \
              "$OUT_TGZ" \
              "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/tmp/nongkung-md.tgz"

      - name: Sync NongKung markdown files into OpenClaw workspace volume
        run: |
          ssh -i ~/.ssh/id_ed25519_hostinger -o StrictHostKeyChecking=no \
            "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" bash -s << 'MD_SYNC_EOF'
          set -euo pipefail

          VOL_PATH=$(docker volume inspect openclaw-sgnl_openclaw-state --format '{{ .Mountpoint }}' 2>/dev/null || true)
          if [ -z "$VOL_PATH" ]; then
            echo "ERROR: Could not resolve openclaw-state volume mountpoint"
            exit 1
          fi

          TARGET_DIR="$VOL_PATH/workspace/bots/nongkung"
          mkdir -p "$TARGET_DIR"

          tar -xzf /tmp/nongkung-md.tgz -C "$TARGET_DIR"
          chown -R 1000:1000 "$VOL_PATH/workspace"

          echo "Synced markdown files to: $TARGET_DIR"
          ls -la "$TARGET_DIR" | head -50

          rm -f /tmp/nongkung-md.tgz
          MD_SYNC_EOF

      # ── Verify HTTPS endpoint is reachable (external check) ───────────────
      - name: Verify HTTPS health endpoint
        run: |
          echo "Checking https://openclaw.yahwan.biz/health ..."
          for i in 1 2 3 4 5; do
            HTTP=$(curl -o /dev/null -sw "%{http_code}" --max-time 15 \
                     https://openclaw.yahwan.biz/health 2>/dev/null || echo "000")
            if [[ "$HTTP" == "200" ]]; then
              echo "✓ Health check passed (HTTP $HTTP)"
              break
            fi
            echo "  attempt $i/5: HTTP $HTTP — retrying in 10s"
            sleep 10
          done
          if [[ "$HTTP" != "200" ]]; then
            echo "WARNING: Health check did not return 200 (last HTTP: $HTTP)"
            echo "Check container logs on VPS:"
            echo "  ssh root@${{ secrets.VPS_HOST }} 'docker logs openclaw-sgnl-openclaw-1 --tail 50'"
            exit 1
          fi

      # ── Upload regression test scripts to VPS ─────────────────────────────
      - name: Upload test scripts to VPS
        run: |
          scp -i ~/.ssh/id_ed25519_hostinger \
              -o StrictHostKeyChecking=no \
              tests/regression-tests.sh \
              tests/r3-regression-tests.sh \
              tests/ws24-regression-tests.sh \
              "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/tmp/"
          echo "Test scripts uploaded to VPS /tmp/"

      # ── Run regression tests on VPS ──────────────────────────────────────
      - name: Run regression tests (infrastructure)
        run: |
          set -o pipefail
          DEPLOY_NUM=${{ github.run_number }}
          LOG_DIR="operations/deployments/deploy-${DEPLOY_NUM}/logs"
          mkdir -p "$LOG_DIR"
          
          ssh -i ~/.ssh/id_ed25519_hostinger -o StrictHostKeyChecking=no \
            "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" \
            'bash /tmp/regression-tests.sh' | tee "$LOG_DIR/regression-tests-output.txt"
          
          # Also copy to /tmp for artifact upload (backward compatible)
          cp "$LOG_DIR/regression-tests-output.txt" /tmp/regression-tests-output.txt

      - name: Run R3 regression tests (LINE + exec + config)
        run: |
          set -o pipefail
          DEPLOY_NUM=${{ github.run_number }}
          LOG_DIR="operations/deployments/deploy-${DEPLOY_NUM}/logs"
          mkdir -p "$LOG_DIR"
          
          ssh -i ~/.ssh/id_ed25519_hostinger -o StrictHostKeyChecking=no \
            "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" \
            'bash /tmp/r3-regression-tests.sh' | tee "$LOG_DIR/r3-regression-tests-output.txt"
          
          cp "$LOG_DIR/r3-regression-tests-output.txt" /tmp/r3-regression-tests-output.txt

      - name: Run WS-2.4 regression tests (session + context + exec + resources)
        run: |
          set -o pipefail
          DEPLOY_NUM=${{ github.run_number }}
          LOG_DIR="operations/deployments/deploy-${DEPLOY_NUM}/logs"
          mkdir -p "$LOG_DIR"
          
          ssh -i ~/.ssh/id_ed25519_hostinger -o StrictHostKeyChecking=no \
            "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" \
            'bash /tmp/ws24-regression-tests.sh' | tee "$LOG_DIR/ws24-regression-tests-output.txt"
          
          cp "$LOG_DIR/ws24-regression-tests-output.txt" /tmp/ws24-regression-tests-output.txt

      # ── On failure: capture diagnostic logs ──────────────────────────────
      - name: Capture diagnostic logs on failure
        if: failure()
        run: |
          DEPLOY_NUM=${{ github.run_number }}
          LOG_DIR="operations/deployments/deploy-${DEPLOY_NUM}/logs"
          mkdir -p "$LOG_DIR"
          
          {
            echo "=== Container logs (last 200 lines, errors only) ==="
            timeout 45 ssh -i ~/.ssh/id_ed25519_hostinger \
              -o StrictHostKeyChecking=no \
              -o ConnectTimeout=20 \
              -o ServerAliveInterval=10 \
              -o ServerAliveCountMax=2 \
              "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" \
              'docker logs openclaw-sgnl-openclaw-1 --tail=200 2>&1 | grep -E "ERROR|Exception|Traceback|FATAL"' || true
            
            echo ""
            echo "=== Container status ==="
            timeout 45 ssh -i ~/.ssh/id_ed25519_hostinger \
              -o StrictHostKeyChecking=no \
              -o ConnectTimeout=20 \
              -o ServerAliveInterval=10 \
              -o ServerAliveCountMax=2 \
              "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" \
              'docker ps -a --filter name=openclaw' || true
            
            echo ""
            echo "=== Nginx config test ==="
            timeout 45 ssh -i ~/.ssh/id_ed25519_hostinger \
              -o StrictHostKeyChecking=no \
              -o ConnectTimeout=20 \
              -o ServerAliveInterval=10 \
              -o ServerAliveCountMax=2 \
              "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" \
              'nginx -t 2>&1' || true
          } > "$LOG_DIR/diagnostic-logs.txt" 2>&1
          
          # Also copy to /tmp for artifact upload
          cp "$LOG_DIR/diagnostic-logs.txt" /tmp/diagnostic-logs.txt
          cat "$LOG_DIR/diagnostic-logs.txt"

      # ── Upload test results as artifacts ──────────────────────────────────
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: regression-test-results
          path: |
            /tmp/regression-tests-output.txt
            /tmp/r3-regression-tests-output.txt
            /tmp/ws24-regression-tests-output.txt
            /tmp/diagnostic-logs.txt
          retention-days: 30

      # ── Commit deployment logs to repo ─────────────────────────────────────
      - name: Commit deployment logs
        if: always()
        run: |
          git config --local user.email "ci@openclaw.dev"
          git config --local user.name "OpenClaw CI"
          
          DEPLOY_DIR="operations/deployments/deploy-${{ github.run_number }}"
          if [ -d "$DEPLOY_DIR/logs" ]; then
            git add "$DEPLOY_DIR/" 2>/dev/null || true
            if git diff --cached --quiet; then
              echo "No changes to commit"
            else
              git commit -m "logs(deploy): capture regression tests from run #${{ github.run_number }}" 2>/dev/null || true
              git push -u origin main 2>/dev/null || true
              echo "✓ Deployment logs committed and pushed"
            fi
          fi

      # ── Cleanup ───────────────────────────────────────────────────────────
      - name: Remove SSH key
        if: always()
        run: rm -f ~/.ssh/id_ed25519_hostinger
