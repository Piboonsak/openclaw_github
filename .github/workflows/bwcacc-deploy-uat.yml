name: BWCAcc Deploy UAT

"on":
  workflow_dispatch:
  push:
    branches: [uat]

concurrency:
  group: bwcacc-deploy-uat
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy BWCAcc UAT
    runs-on: ubuntu-latest
    environment: uat
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test step
        run: echo "deploy would run here"
